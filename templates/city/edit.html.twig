{% extends 'base.html.twig' %}

{% block title %}Edit City{% endblock %}

{% block body %}
<div class="container productForm">
    <h1 class="text-center mt-4 mb-4">Edit City</h1>
    <a href="{{ path('app_city_index') }}" class="btn btn-outline-light mb-2">back to list</a>
    {{ include('city/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('city/_delete_form.html.twig') }}
</div>
{% endblock %}
